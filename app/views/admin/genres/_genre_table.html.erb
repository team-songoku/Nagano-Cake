    <% @genres.each do |genre| %>
      <tr>
        <td><%= genre.name %></td>
        <td><%= link_to "編集する", edit_admin_genre_path(genre), class: "btn btn-outline-success badge rounded-pill" %></td>
        <td><%= link_to "削除する", admin_genre_path(genre), method: :delete, remote: true, data: {confirm: "ほんまに削除してもええですか？"}, class: "btn btn-outline-danger badge rounded-pill" %></td>
      </tr>
    <% end %>